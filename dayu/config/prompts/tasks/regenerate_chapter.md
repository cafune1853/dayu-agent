当前任务：
- 整章重建。
- 目标不是润色旧稿，而是基于下方“章节结构”“章节合同”和“修复合同”重新生成一版合规正文。
- 必须优先修掉结构性问题（如缺主节、缺证据小节、骨架错位），再处理局部证据或风格问题。
- 若“是否允许补入新事实”为 true，可在正常检索边界内补入支撑本章最小判断链所必需的新事实；若“是否允许补入新事实”为 false，不得为补齐内容而新增事实，只能重组既有事实、删弱 unsupported claim 或按规则留占位符。

重建原则（强制）：
- 基于下方明确给出的“公司业务类型与关键约束”“全文目标”“读者画像”“本章回答”“章节结构”“章节合同”“条件写作规则”“修复合同”和“上轮正文”来工作。
- 严格按下方“章节结构”的标题顺序输出，不增删一级标题。
- 只覆盖“章节合同”要求的最低必需内容；不要借机扩写。
- 再判断“条件写作规则”中每条 `ITEM_RULE` 的 `when` 是否成立：
  - 不成立时整项不输出，不解释、不占位。
  - 即使成立但无内容可输出时，整项不输出，不解释、不占位。
  - 成立且有内容可输出时，按照“bullet格式要求”作为独立 bullet 输出。
  - `ITEM_RULE.item` 是内部写作提示，不是必须原样抄给读者看的标签；应把它改写成自然、可读、面向读者的 bullet 文案。
  - 若某条 `ITEM_RULE` 触发后，后面写不出至少一句完整、具体、可读的回答，就整项不输出；严禁只留下规则名、半句提示或空标签。
  - `ITEM_RULE` 只能增补正文，不能替代章节骨架里原本就要写的 bullet。
- 正文组织服从“章节合同”的叙事方式，但以可读性和判断力为先。
- 先写能直接支撑判断的事实、结构关系和直接依据；避免空泛分析句、大段摘抄、逐句翻译和“公司披露/根据/报告称/原文指出”等引用套话。

缺口处理：
- 当某个信息在当前正文中必须保留，或某个信息属于本章最小判断链，但暂时无法给出“证据与出处”来源时，该信息位置必须按占位符统一格式规范化输出。
- 占位符统一格式：【占位符】（缺口：{缺失的具体信息} ｜ 需要：{需要什么类型的来源} ｜ 已检索范围：{已检索过的文档/章节} ｜ 下一步：{建议的补强路径}）

输出要求：
- 仅输出 1 个 Markdown 代码块（且只输出这一块）。
  - 代码块内禁止用数字编号（不要出现“1.”“（1）”等）编排格式，可用无序列表与小标题；“章节结构”原本带带编排格式除外。
  - 代码块内禁止用粗体、斜体等编排格式；“章节结构”原本带带编排格式除外。
  - 不输出任何 HTML 注释内容（<!-- ... -->）。
- 仅输出该章节正文，禁止输出其它解释性文字。
- 本章末尾必须包含“### 证据与出处”。
- bullet格式要求（必须遵守）：
  - bullet下另起一行输出回答：第一行bullet，另起一行输出回答正文，二者之间不留空行。
  - 严禁把bullet改写成非bullet。
  - 不得在bullet行直接追加回答内容、解释文字或额外标点；禁止写成 `- 标签：回答`、`- 标签 回答` 或 `- 标签：`。
  - 不得输出“只有 bullet、没有正文”的空条目；任何 bullet 后面都必须紧跟至少一句完整回答。
  - 不得把 `ITEM_RULE.item` 原样当作裸标签抄出，除非它本身已经是自然中文且后面紧跟完整回答。
  - 相邻 bullet 之间保留空行。

章节标题：
{{chapter}}

公司业务类型与关键约束：
{{company_facets_summary_block}}

全文目标：
{{report_goal}}

读者画像：
{{audience_profile}}

本章回答：
{{chapter_goal}}

章节结构：
{{skeleton_block}}

章节合同：
{{chapter_contract_block}}

条件写作规则：
{{item_rules_block}}

是否允许补入新事实：
{{allow_new_facts}}

本轮修复范围：
{{retry_scope}}

修复合同：
{{last_repair_contract_block}}

上轮正文（仅供识别需避开的错误写法，不要求沿用）：
{{last_wrote_content_block}}

上轮审计结果（以修复合同为准，此处仅供参考）：
{{last_audit_json_block}}
