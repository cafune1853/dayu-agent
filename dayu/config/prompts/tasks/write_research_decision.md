写作目标：
- 章节为“是否值得继续深研与待验证问题”。
- 基于传入的前文章节结构化输入，输出最终的研究决策：`继续研究 / 暂缓 / 放弃`。
- 这不是摘要回填，也不是重新研究输入里已有的章节内容；你的职责是综合当前输入，先决定当前研究动作，再在必要时补足支撑该动作所需的最小缺口。

输出要求：
- 仅输出 1 个 Markdown 代码块（且只输出这一块）。
  - 代码块内禁止用数字编号（不要出现“1.”“（1）”等）编排格式，可用无序列表与小标题。
  - 代码块内禁止用粗体、斜体等编排格式。
  - 不输出任何 HTML 注释内容（<!-- ... -->）。
- 仅输出该章节正文，禁止输出其它解释性文字。
- 本章末尾必须包含“### 证据与出处”。
- bullet格式要求：
  - bullet下另起一行输出回答：第一行bullet，另起一行紧接着输出回答正文，二者之间不留空行。
  - 不得把bullet改写成非bullet。
  - 不得在bullet行直接追加回答内容、解释文字或额外标点；禁止写成 `- 标签：回答`、`- 标签 回答` 或 `- 标签：`。
  - 相邻 bullet 之间保留空行；不要把多个字段合并进同一 bullet，也不要把单个字段再拆成内层 bullet，除非下方章节结构已明确要求。

内容边界（强制）：
- 下方“前文章节结构化输入”是本章的主上下文；先用它完成当前研究动作，不要一上来重新检索全公司。
- 当前输入允许使用的口径边界已在下方给出；若当前输入已足够支撑判断，不要新增事实或来源。
- 若下方写明允许新增事实，只允许补入服务当前研究动作所需的最小必要事实；不得重新展开一轮章节研究。
- 若下方写明允许新增来源，只允许引入服务当前研究动作所需的最小必要来源；不得顺手补充无关背景资料。
- 若当前输入与新增事实之间存在冲突，优先显式写出“不确定性 / 待验证项”，不要假装冲突不存在。
- 本章必须基于传入的前文章节结构化输入形成当前判断，但不得把输出写成信息罗列式摘要或机械改写，也不得把“不确定”伪装成“放弃”。

决策原则：
- 先判断当前输入是否足以支持明确拍板；
  - 若足以拍板，再给出 `继续研究 / 暂缓 / 放弃` 的明确动作。
  - 若当前输入存在关键冲突或关键缺口，无法安全拍板，应明确写出“当前更倾向于...”的暂定动作，并把未决项写成待验证问题或最小重启条件；不要为了显得果断而硬下结论。
- 再把当前动作压缩成最小判断链：
  - 默认只写 1 条主判断链；只有在第二条判断链独立且缺一不可时，才写第 2 条。
  - 不要把“支撑依据”写成优点清单、投资亮点清单，或当前输入里所有正面信息的堆砌。
- 再写当前最关键的反对依据或卡点，解释它为什么足以让当前动作不能更积极。
  - 默认只写一个主卡点；只有在两个卡点彼此独立且同等重要时，才允许并列。
  - `主卡点` 应写当前最阻止升级或维持当前动作的现实阻碍；不要写成“如果未来变好，当前动作可能过于保守”这类反事实句。
- 若动作为“继续研究”，必须给出下一轮最该验证的问题；默认先写 1 个最关键问题，只有额外问题也会单独改变动作时才补到 2–3 个。
  - 不要把两个问题并列塞进同一项，除非两个问题彼此独立且任何一个单独成立都会改变当前动作。
- 无论动作是“继续研究 / 暂缓 / 放弃”，都必须写清“什么变化会升级、降级或终止当前动作”；不要写“不适用”。
- 把“什么变化会改变当前动作”尽量收敛成真正的阈值事件：
  - 默认写 1 个升级阈值；
  - 再写 1 个降级或终止阈值；
  - 不要写成长串条件并列清单。
- 若动作为“继续研究”，最后一项要回答：出现哪些新证据、坏结果或关键变量恶化时，动作会降级为“暂缓 / 放弃”。
- 若动作为“暂缓 / 放弃”，最后一项要回答：出现哪些新证据或条件改善时，动作会升级或恢复研究。
- 若关键卡点尚未证实，应写成“待验证问题”或“最小重启条件”，不要把缺证事实写成硬结论。

写法收口：
- `使该动作成立的最小判断链` 默认只写 1 条主判断链；只有在第二条判断链独立且缺一不可时，才保留第 2 条。
- `当前最可能让该动作判断出错的卡点` 默认只写一个主卡点；不要把多个风险并列成清单，也不要再拆成多层 bullet。
- `下一轮最小验证计划` 默认先写 1 个最关键问题；只有额外问题也会单独改变动作时，才补到 2–3 个。
- `什么变化会升级、降级或终止当前动作` 默认写两个阈值事件：
  - 一个最关键的升级阈值；
  - 一个最关键的降级或终止阈值。

章节标题：
{{chapter}}

公司名称：
{{company}}

股票代码：
{{ticker}}

公司业务类型与关键约束：
{{company_facets_summary_block}}

全文目标：
{{report_goal}}

读者画像：
{{audience_profile}}

本章回答：
{{chapter_goal}}

当前输入口径：
{{decision_source_of_truth}}

是否允许补入新事实：
{{decision_allow_new_facts}}

是否允许新增来源：
{{decision_allow_new_sources}}

章节结构：
{{skeleton_block}}

章节合同：
{{chapter_contract_block}}

条件写作规则：
{{item_rules_block}}

前文章节结构化输入：
{{prior_chapters_input_block}}
