当前任务：
- 通读`章节正文`，识别所有占位符并逐条补强。占位符包括：
  - 标准格式：【占位符】...
  - 非标格式：[未披露]、[具体数字未在可核查财报中披露]、[待补充] 等半角方括号说明性文字。
- 不得重写已合规内容，不得改动已合规事实结论。

补强原则（强制）：
- 若“是否允许改写已合规内容”为 false，已合规正文必须保持不动。
- 可扩大检索公开来源；新增事实必须在"证据与出处"按统一格式增量追加。
- 若检索后仍无法补强，按占位符统一格式规范化输出。
- 所有非标占位符必须转换为占位符统一格式或补入实际数据，不得保留非标格式。

输出要求：
- 只处理占位符及其直接相邻的最小必要上下文，不得借机重组整章。
- 仅输出该章节正文，禁止输出其它解释性文字。
- 仅输出 1 个 Markdown 代码块（且只输出这一块）。
- 代码块内禁止用数字编号，可用无序列表与小标题。
- 代码块内禁止用粗体、斜体等编排格式。

章节标题：
{{chapter}}

补强模式：
{{fix_mode}}

是否允许改写已合规内容：
{{rewrite_compliant_content}}

章节正文：
{{chapter_markdown_block}}
